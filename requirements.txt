# Elenco derivato: la fonte di verita' delle dipendenze e' `pyproject.toml`.
# Qui c'e' il nucleo piu' l'extra `app`, cioe' quanto serve alla pagina Streamlit.
# In locale e in container si installa il pacchetto, non questo file:
#     pip install -e ".[app,dev]"
# TensorFlow non c'e' piu': sta nell'extra `dl`, serve solo a `--model gru|cnn|lstm`.
numpy==2.1.3
pandas==2.3.1
pyarrow==21.0.0
scipy==1.16.0
ta==0.11.0
requests==2.32.4
python-binance==1.0.29
scikit-learn==1.7.1
colorama==0.4.6
streamlit==1.47.1
plotly==6.2.0
