# Handoff di sessione — CryptoFarm, 2026-08-21

Repo: `/Users/marcomonini/PycharmProjects/CryptoFarm` · branch `ai-labeling-rewrite`
(34 commit avanti su `main`, mai fatto merge) · **working tree pulito, tutto committato**.

## Leggi questi, non riassumerli

| documento | cosa contiene |
|---|---|
| `.claude/docs/HANDOFF.md` | stato del lavoro, cosa NON rifare, trappole ambientali, regole di ingaggio dell'utente. **È aggiornato al termine di questa sessione** — questo file non lo duplica, lo integra con ciò che stava solo nella conversazione. |
| `.claude/docs/strategy.md` §10–13 | tutte le misure e il perché del risultato negativo |
| `CLAUDE.md` | architettura, comandi, ambiente |
| `git log main..HEAD` | i messaggi spiegano ogni scelta e ogni bug trovato |

Trascritto completo della sessione precedente, se serve un dettaglio esatto:
`~/.claude/projects/-Users-marcomonini-PycharmProjects-CryptoFarm/c6189033-2dd1-4d1f-a02c-2b98d1e91a61.jsonl`

## Dove si è fermata la conversazione

I 5 punti dell'ordine di lavoro dell'utente sono **tutti eseguiti**. Il risultato è negativo e la
causa è aritmetica, non un difetto del modello (§13, "tassa di conferma"). L'ultima richiesta
dell'utente — far girare il modello nel simulatore per osservarne il comportamento — è **completata
e committata** (`bacd384`).

Nessun task in sospeso. La prossima sessione riparte da una decisione dell'utente, non da un
lavoro interrotto.

## Se l'utente vuole proseguire la ricerca

L'unica direzione aperta è in `.claude/docs/strategy.md` §13.4 (riformulazione del target come
classificazione binaria alla barra di conferma). **È una direzione nuova, non la continuazione di
una richiesta già data: va confermata con l'utente prima di iniziare.** L'utente ha chiesto piano
scritto + conferma prima di modifiche strutturali, salvo quando dice esplicitamente "procedi con
l'implementazione".

## Se l'utente vuole guardare il simulatore

```bash
streamlit run src/cryptofarm/trading/simulator.py     # strategia "AI Model"
```

Tre avvertenze già date all'utente, da ripetere se rileggono i numeri:
- il simulatore applica **0,1% per lato**, contro lo 0,08% andata-e-ritorno maker della §12 →
  il P&L mostrato è peggiore di quello riportato. Per il confronto giusto: `fee_percent` a 0,04.
- il modello è addestrato su **5m**, il simulatore gira anche su 15m.
- **il modello perde fuori campione**: guardarlo è diagnostica (dove entra ed esce), non speranza.

Misure fatte per verificare l'integrazione: BTC 15m dal 2026-01 → 176 operazioni, lordo medio
+0,004%, mediano −0,376%, 39,2% in utile. ETH 15m dal punto di ingresso del simulatore → 180
segnali accoppiati su 16.481 candele.

## Stato tecnico

- **115 test verdi.** `.venv312/bin/python -m pytest`
- `ruff check src tests`: i 5 rilievi in `trading/simulator.py` sono **pre-esistenti**.
- Ambiente: **`.venv312/bin/python`** (il `.venv` è Python 3.9 senza scikit-learn).
- `models/policy_model.*` (copia di `policy_alta.*`) è quello che il simulatore carica.
  **Non cancellare `models/meta_model.*`**: è il modello della strategia precedente.
- Nessuna credenziale né dato personale toccato in questa sessione.

## Suggested skills

Da invocare col tool `Skill`, a seconda di cosa chiede l'utente:

- **`tdd`** — se si implementa il target di §13.4. In questa sessione i due bug più costosi
  (leakage DAgger dentro la CPCV, appaiamento delle operazioni fra episodi) sono stati trovati
  dai test, non dall'ispezione.
- **`diagnosing-bugs`** — quando una misura non torna. Serve la disciplina del controllo causale:
  in questa sessione un'attribuzione con uscita "perfetta" stava per far concludere il contrario
  del vero.
- **`dataviz`** — prima di aggiungere grafici (`app/analysis_dashboard.py` è ora in `backup/unused/`).
- **`artifact-design`** — solo se l'utente chiede un report visuale pubblicabile.

Non servono `research` (nessuna fonte esterna) né `codebase-design` (struttura già decisa,
`strategy.md` §8).
