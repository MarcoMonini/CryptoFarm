# 📈 CryptoFarm

**CryptoFarm** is a web-based application designed to simulate cryptocurrency trading strategies. It connects to the **Binance API** to retrieve real-time market data and includes a machine learning module to train predictive models on historical data.

---

## 📁 Project Structure

### `simulator.py`
- Web application built with [Streamlit](https://streamlit.io/)
- Features:
  - Simulates **buy/sell** trading actions in real-time
  - Fetches live cryptocurrency data using the **Binance API**
  - Provides real-time charts and data visualization
  - Allows users to **download historical data** via a dedicated button in the interface
- Useful for testing and comparing different trading strategies in a safe environment

### `cryptoTrainerAI.py`
- Python script for training a **machine learning model** using past data
- Utilizes the historical data CSV file generated by `simulator.py`
- Trains a predictive model to analyze patterns and improve trading strategy performance

---

## ⚙️ Requirements

Install all dependencies using the provided `requirements.txt`:

```bash
pip install -r requirements.txt

